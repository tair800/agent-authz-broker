"""agent-authz-broker: an MCP resource server that computes authority instead of accepting it.

See `DECISIONS.md` ADR-001 — the claim, the threat model and the kill test, all fixed before
implementation.
"""

__version__ = "0.1.0"
